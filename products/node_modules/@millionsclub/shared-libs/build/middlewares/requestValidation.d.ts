import { Request, Response, NextFunction } from 'express';
import { ZodType } from 'zod/v4';
export declare const validateRequest: (schema: ZodType) => (req: Request, _res: Response, next: NextFunction) => void;
